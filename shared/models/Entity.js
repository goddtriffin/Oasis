// Entity model
// (Location) location , (Size) size, (double) speed
class Entity {
    constructor (location, size, speed) {
        this.location = location;
        this.size = size;
        this.speed = speed;
    }
}